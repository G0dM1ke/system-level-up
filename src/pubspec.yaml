name: system_level_up
description: SYSTEM - Level Up | Dungeon Daily | Secret Mode
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.3
  flutter_secure_storage: ^9.2.2
  local_auth: ^2.2.0
  intl: ^0.19.0

flutter:
  uses-material-design: true
